# Hybrid Quantum-Classical Neural Network Dependencies
# Last Updated: February 2026
# Python Version: 3.12.x (recommended) or 3.13.x
# Platform: Google Colab Pro (A100 GPU) or M4 Mac (development)

# ============================================
# Core ML & Deep Learning
# ============================================
torch>=2.6.0
torchvision>=0.21.0

# ============================================
# Quantum Machine Learning
# ============================================
pennylane>=0.44.0

# GPU-accelerated quantum simulator (CUDA required)
# Only for Google Colab / NVIDIA GPU systems
# M4 Mac automatically uses default.qubit (CPU simulator)
pennylane-lightning-gpu>=0.43.0; platform_system != "Darwin"

# ============================================
# Scientific Computing
# ============================================
numpy>=2.0.0  # PennyLane 0.44 requires NumPy 2.x
scipy>=1.11.0

# ============================================
# Machine Learning Utilities
# ============================================
scikit-learn>=1.5.0
matplotlib>=3.9.0
tqdm>=4.66.0

# ============================================
# Data Processing
# ============================================
opencv-python-headless>=4.10.0  # headless = no GUI deps
Pillow>=10.4.0

# ============================================
# Experiment Tracking & Visualization
# ============================================
tensorboard>=2.17.0
wandb>=0.18.0

# ============================================
# Configuration & Utilities
# ============================================
PyYAML>=6.0.1
python-dotenv>=1.0.0

# ============================================
# Platform Notes
# ============================================
#
# GOOGLE COLAB PRO (Training):
#   pip install -r requirements.txt
#   Uses lightning.gpu + CUDA (auto-detected)
#
# M4 MAC MINI (Development):
#   pip install -r requirements.txt
#   Uses default.qubit CPU (auto-detected)
#   Skip pennylane-lightning-gpu (excluded via platform marker)
