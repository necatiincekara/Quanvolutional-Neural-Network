# V7 Artifact Sync Checklist

**Date:** April 7, 2026

This checklist defines the zero-compute follow-up work after the fresh Colab L4 V7 rerun that reached `72.89%` best validation and `72.53%` test accuracy.

## Goal

Replace the current log-reconciled local V7 record with the actual remote artifacts produced by the rerun.

## Current Local Status

- The local repo already contains a reconciled JSON row:
  - `experiments/v7_trainable_quantum_rerun_20260406_l4.json`
- That row is based on the captured Colab terminal log.
- The local repo already contains the synced checkpoint files:
  - `models/checkpoint_latest_v7.pth`
  - `models/best_v7_model.pth`
- The actual remote rerun folder is still missing from the local workspace.

## Expected Remote Artifacts

From the Colab workspace and Drive backup path, preserve and sync the following if they exist:

- `models/checkpoint_latest_v7.pth`
- `models/best_v7_model.pth`
- `experiments/v7_data_reuploading_20260406_190203/`
- any `metadata.json` inside that experiment folder
- any training-curve image generated by the rerun

From Drive backup:

- `/content/drive/MyDrive/quanv_results/v7/checkpoint_latest_v7.pth`
- `/content/drive/MyDrive/quanv_results/v7/best_v7_model.pth`

## Minimal Sync Procedure

1. Confirm the synced checkpoint files remain present under `models/`.
2. Copy the rerun experiment folder from Colab if it is still available.
3. Place any additional synced files into the local repo under:
   - `models/`
   - `experiments/`
4. Do not overwrite unrelated historical checkpoints without confirming filenames.

## After Sync

Once the files are local:

1. Verify the filenames and timestamps.
2. Confirm that the synced artifacts are consistent with:
   - `experiments/v7_trainable_quantum_rerun_20260406_l4.json`
   - `docs/EXPERIMENTS.md`
   - `docs/BENCHMARK_SUMMARY.md`
   - `paper/draft.md`
3. If the remote artifact names differ from the current reconstructed JSON row, update the notes so the record is no longer described as log-reconciled only.

## Stop Condition

This checklist is complete when:

- the rerun is backed by local checkpoint files and, ideally, the remote experiment folder,
- the benchmark docs no longer need to warn that the rerun is represented only by a reconstructed terminal-log row,
- no additional Colab compute has been spent.
